#include <bits/stdc++.h>
using ll = long long;
using namespace std;

int main() {
    int q;
    cin >> q;
    while(q--) {
        ll a, b, c;
        cin >> a >> b >> c;
        cout << (a + b + c) / 2 << endl;
    }
    return 0;
}
